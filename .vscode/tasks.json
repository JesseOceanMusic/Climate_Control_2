{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build with CMake",
      "type": "shell",
      "command": "cd build && ctest --output-on-failure -V",
      "problemMatcher":[
        "$gcc"
      ],
      "group": {
        "kind": "test",
        "isDefault": true,
        "detail": "compiler: /usr/bin/gcc"
      }
    }
  ]
}